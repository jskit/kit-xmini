import {
  log,
} from '../mini/utils';

exports.debug = function() {
  // 调试问题
}

exports.init = function(opts = {}) {
  log.warn(':::x-mini add debug');
  const {
    xApp,
    xPage,
  } = opts;
}
